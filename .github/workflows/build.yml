name: Build
on:
  workflow_dispatch:
    inputs:
      app:
        type: choice
        description: App type
        options: 
        - admin
        - seller
        - buyer
      environment:
        type: choice
        description: Environment
        options: 
        - production
        - staging
        - demo